const db = require('../data/db-config')

module.exports = {
  find,
}

function find() {
  // return db("items as i")
  // .join('users as u','u.user_id', 'i.user_id' )
  // .select('i.*', 'u.user_id')
}


